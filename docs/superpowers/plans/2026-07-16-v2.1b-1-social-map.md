# v2.1b-1 소셜 지도 레이어 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 친구가 저장한 곳을 지도에 오버레이(20 상한 무관)하고 핀을 3상태(나만/친구만+파란 숫자 배지/겹침+링)로 구분한다.

**Architecture:** Supabase에 `friend_saves_near(lat,lng,radius)` 집계 함수(+saved_places RLS 친구확장)를 두고, `SocialRepository`가 뷰포트 친구-저장을 가져온다. 순수 헬퍼 `friendOverlay`가 친구-저장을 지도 핀에 병합하고 place_id별 friend_count 맵을 만든다. `place_pin.dart`의 `SavedPin`을 `PlacePin({saved, friendCount})`로 일반화해 북마크·파란 배지·링을 렌더한다. `MapController.setFriendCounts`로 지도에 카운트를 주입한다.

**Tech Stack:** Flutter 3.44.6, Postgres(RLS+SQL 집계), 기존 v2.0d 오버레이·v2.0b-2 핀·v2.1a 친구그래프 재사용. 새 dart 의존성 없음.

## Global Constraints
- 저장소 `/Users/goodbug/dev/odagada`, 브랜치 `feature/v2b-save`(현재), 새 브랜치 금지. Flutter `/opt/homebrew/bin`(`export PATH="/opt/homebrew/bin:$PATH"`). 패키지 `odagada`.
- 기존 68 테스트 회귀 유지. 순수 모델/헬퍼/위젯 TDD. `flutter analyze` 신규 0(기존 info 3건).
- **저장소 구현은 무테스트(E2E)** — Supabase 목킹 안 함(코드베이스 관례).
- **마이그레이션 `0004_social.sql`은 사용자가 Supabase 대시보드 적용**(0001~0003처럼).
- 친구 색(배지·링) = `#1B4F9B`(카테고리 쇼핑색 #2E7DF6과 구분되게 더 진한 파랑). 스펙 §7.7 핀 3상태.
- 통합 정보카드(누가+메모)는 범위 제외(v2.1b-2). 이 슬라이스는 배지·링까지.
- 친구 저장은 뷰포트 20 상한과 무관하게 오버레이(내 저장 오버레이와 동일 원칙).

## 파일 구조
- Create `supabase/migrations/0004_social.sql`
- Create `lib/friends/friend_save.dart` — `FriendSave` 모델 + `friendSaveFromRow` + `friendOverlay` 순수 헬퍼
- Create `lib/friends/social_repository.dart` — `SocialRepository`(추상) + `SupabaseSocialRepository`
- Modify `lib/ui/place_pin.dart` — `SavedPin`→`PlacePin({saved,friendCount})`(신규 추가; SavedPin 제거는 Task4)
- Modify `lib/ui/map_view.dart`, `lib/ui/google_map_view.dart`, `lib/ui/kakao_map_view.dart` — setFriendCounts + 핀 분기
- Modify `lib/ui/map_screen.dart`, `lib/app.dart` — socialRepo 주입·병합
- Test `test/friends/friend_save_test.dart`, `test/ui/place_pin_test.dart`(케이스 추가)

---

### Task 1: 마이그레이션 `0004_social.sql`

**Files:** Create `supabase/migrations/0004_social.sql`

**Interfaces:** Produces DB: `saved_places` 친구 조회 정책; 함수 `friend_saves_near(double precision,double precision,double precision)`.

- [ ] **Step 1: SQL 작성**

`supabase/migrations/0004_social.sql`:
```sql
-- v2.1b-1 소셜 지도: saved_places 친구 조회 + 뷰포트 친구-저장 집계

-- 친구는 accepted 친구의 is_public 저장 조회 가능(0002 본인정책에 OR로 추가).
create policy saved_places_friends_select on saved_places for select
  using (
    owner_id = auth.uid() or (
      is_public and exists (
        select 1 from friendships f
        where (f.user_a = auth.uid() and f.user_b = saved_places.owner_id)
           or (f.user_b = auth.uid() and f.user_a = saved_places.owner_id)
      )
    )
  );

-- 뷰포트(center+radius bbox 근사)의 친구-저장을 place_id로 그룹 집계.
create or replace function friend_saves_near(
  p_lat double precision, p_lng double precision, p_radius double precision)
returns table(
  place_id text, lat double precision, lng double precision,
  name text, category text,
  friend_count int, friend_names text[], memos text[])
language sql security definer set search_path = public as $$
  select sp.place_id,
         max(sp.lat), max(sp.lng), max(sp.name), max(sp.category),
         count(*)::int,
         array_agg(coalesce(p.display_name, '친구') order by sp.owner_id),
         array_agg(coalesce(sp.memo, '') order by sp.owner_id)
  from saved_places sp
  join friendships f
    on (f.user_a = auth.uid() and f.user_b = sp.owner_id)
    or (f.user_b = auth.uid() and f.user_a = sp.owner_id)
  join profiles p on p.id = sp.owner_id
  where sp.place_id is not null and sp.is_public
    and sp.owner_id <> auth.uid()
    and sp.lat between p_lat - (p_radius/111000.0)
                   and p_lat + (p_radius/111000.0)
    and sp.lng between p_lng - (p_radius/(111000.0*cos(radians(p_lat))))
                   and p_lng + (p_radius/(111000.0*cos(radians(p_lat))))
  group by sp.place_id;
$$;

grant execute on function friend_saves_near(double precision,double precision,double precision) to authenticated;
```

- [ ] **Step 2: 커밋**
```bash
cd /Users/goodbug/dev/odagada
git add supabase/migrations/0004_social.sql
git commit -m "feat(db): social map migration (saved_places friend RLS + friend_saves_near)"
```
(사용자가 대시보드 적용 — E2E 전제.)

---

### Task 2: FriendSave 모델 + friendSaveFromRow + friendOverlay + SocialRepository

**Files:**
- Create: `lib/friends/friend_save.dart`, `lib/friends/social_repository.dart`
- Test: `test/friends/friend_save_test.dart`

**Interfaces:**
- Consumes: `PlaceCategory.fromId`, `Poi`, `LatLng`.
- Produces: `class FriendSave{String placeId; double lat,lng; String name; PlaceCategory bucket; int friendCount; List<String> friendNames; List<String> memos}`; `FriendSave friendSaveFromRow(Map)`; `({List<Poi> extraPins, Map<String,int> friendCounts}) friendOverlay(List<FriendSave>, Set<String> existingIds)`; `abstract class SocialRepository { Future<List<FriendSave>> friendSavesNear(LatLng center, double radiusMeters); }` + `SupabaseSocialRepository`.

- [ ] **Step 1: 실패하는 테스트**

`test/friends/friend_save_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:odagada/core/models/lat_lng.dart';
import 'package:odagada/core/models/place_category.dart';
import 'package:odagada/friends/friend_save.dart';

void main() {
  test('friendSaveFromRow 매핑(category→bucket, 배열)', () {
    final fs = friendSaveFromRow({
      'place_id': 'p1', 'lat': 37.5, 'lng': 127.0, 'name': '금양화로',
      'category': 'restaurant', 'friend_count': 2,
      'friend_names': ['철수', '영희'], 'memos': ['맛있어', ''],
    });
    expect(fs.placeId, 'p1');
    expect(fs.bucket, PlaceCategory.restaurant);
    expect(fs.friendCount, 2);
    expect(fs.friendNames, ['철수', '영희']);
    expect(fs.memos, ['맛있어', '']);
  });

  test('friendOverlay: 카운트 전부, extraPins는 기존에 없는 것만', () {
    final saves = [
      FriendSave(placeId: 'a', lat: 37.5, lng: 127.0, name: 'A',
          bucket: PlaceCategory.cafe, friendCount: 1, friendNames: ['철수'], memos: ['']),
      FriendSave(placeId: 'b', lat: 37.6, lng: 127.1, name: 'B',
          bucket: PlaceCategory.bar, friendCount: 3, friendNames: [], memos: []),
    ];
    final r = friendOverlay(saves, {'a'}); // 'a'는 이미 지도에 있음
    expect(r.friendCounts, {'a': 1, 'b': 3});
    expect(r.extraPins.map((p) => p.id).toList(), ['b']); // b만 추가
    expect(r.extraPins.single.bucket, PlaceCategory.bar);
  });
}
```

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/goodbug/dev/odagada && flutter test test/friends/friend_save_test.dart`
Expected: FAIL(파일 없음).

- [ ] **Step 3: 구현**

`lib/friends/friend_save.dart`:
```dart
import '../core/models/lat_lng.dart';
import '../core/models/place_category.dart';
import '../core/models/poi.dart';

/// 친구가 저장한 곳(place_id로 집계). 지도 소셜 오버레이용.
class FriendSave {
  final String placeId;
  final double lat;
  final double lng;
  final String name;
  final PlaceCategory bucket;
  final int friendCount;
  final List<String> friendNames;
  final List<String> memos;
  const FriendSave({
    required this.placeId,
    required this.lat,
    required this.lng,
    required this.name,
    required this.bucket,
    required this.friendCount,
    required this.friendNames,
    required this.memos,
  });
}

FriendSave friendSaveFromRow(Map<String, dynamic> row) => FriendSave(
      placeId: row['place_id'] as String,
      lat: (row['lat'] as num).toDouble(),
      lng: (row['lng'] as num).toDouble(),
      name: (row['name'] as String?) ?? '이름 없음',
      bucket: PlaceCategory.fromId(row['category'] as String?),
      friendCount: (row['friend_count'] as num?)?.toInt() ?? 0,
      friendNames: ((row['friend_names'] as List?) ?? const [])
          .map((e) => e as String)
          .toList(),
      memos: ((row['memos'] as List?) ?? const [])
          .map((e) => e as String)
          .toList(),
    );

/// 친구-저장을 지도 핀에 병합할 재료를 만든다.
/// friendCounts: place_id별 친구 수(모든 친구-저장). extraPins: 지도에 아직
/// 없는(검색결과·내저장에 없는) 친구-only 장소를 Poi로 변환한 것.
({List<Poi> extraPins, Map<String, int> friendCounts}) friendOverlay(
    List<FriendSave> friendSaves, Set<String> existingIds) {
  final counts = <String, int>{};
  final extra = <Poi>[];
  for (final fs in friendSaves) {
    counts[fs.placeId] = fs.friendCount;
    if (!existingIds.contains(fs.placeId)) {
      extra.add(Poi(
        id: fs.placeId,
        name: fs.name,
        position: LatLng(fs.lat, fs.lng),
        category: fs.bucket.label,
        bucket: fs.bucket,
        distanceMeters: 0,
      ));
    }
  }
  return (extraPins: extra, friendCounts: counts);
}
```

`lib/friends/social_repository.dart`:
```dart
import 'package:supabase_flutter/supabase_flutter.dart';
import '../core/models/lat_lng.dart';
import 'friend_save.dart';

/// 소셜(친구 저장) 조회 저장소 인터페이스.
abstract class SocialRepository {
  Future<List<FriendSave>> friendSavesNear(LatLng center, double radiusMeters);
}

class SupabaseSocialRepository implements SocialRepository {
  final SupabaseClient _client;
  SupabaseSocialRepository([SupabaseClient? client])
      : _client = client ?? Supabase.instance.client;

  @override
  Future<List<FriendSave>> friendSavesNear(
      LatLng center, double radiusMeters) async {
    final rows = await _client.rpc('friend_saves_near', params: {
      'p_lat': center.lat,
      'p_lng': center.lng,
      'p_radius': radiusMeters,
    }) as List;
    return rows
        .map((r) => friendSaveFromRow(r as Map<String, dynamic>))
        .toList();
  }
}
```

- [ ] **Step 4: 통과 + 회귀**

Run: `cd /Users/goodbug/dev/odagada && flutter test test/friends/friend_save_test.dart && flutter test && flutter analyze`
Expected: 신규 PASS, 전건 green, analyze 신규 0.

- [ ] **Step 5: 커밋**
```bash
cd /Users/goodbug/dev/odagada
git add lib/friends/friend_save.dart lib/friends/social_repository.dart test/friends/friend_save_test.dart
git commit -m "feat: FriendSave model + friendOverlay helper + SocialRepository"
```

---

### Task 3: PlacePin (핀 3상태 위젯)

**Files:**
- Modify: `lib/ui/place_pin.dart`(PlacePin 추가; SavedPin은 Task4까지 유지)
- Test: `test/ui/place_pin_test.dart`(케이스 추가)

**Interfaces:**
- Produces: `class PlacePin extends StatelessWidget { PlacePin({required Poi poi, required bool saved, required int friendCount, required VoidCallback onTap}) }`. 앵커는 SavedPin과 동일(머리 중심 x, 꼬리 끝 상단 46px). 렌더: teardrop(bucket 색·아이콘) + saved면 흰 북마크 + friendCount>0이면 파란(`#1B4F9B`) 숫자 배지(좌상단) + (saved&&friendCount>0)이면 파란 링.

- [ ] **Step 1: 실패하는 위젯 테스트 추가**

`test/ui/place_pin_test.dart`에 추가(기존 `_poi()`/`_pump` 헬퍼 재사용):
```dart
  testWidgets('PlacePin: 내저장만 → 북마크, 배지·링 없음', (t) async {
    await _pump(t, PlacePin(poi: _poi(), saved: true, friendCount: 0, onTap: () {}));
    expect(find.byIcon(Icons.bookmark), findsOneWidget);
    expect(find.text('2'), findsNothing);
  });

  testWidgets('PlacePin: 친구만 → 파란 숫자 배지, 북마크 없음', (t) async {
    await _pump(t, PlacePin(poi: _poi(), saved: false, friendCount: 2, onTap: () {}));
    expect(find.text('2'), findsOneWidget);
    expect(find.byIcon(Icons.bookmark), findsNothing);
  });

  testWidgets('PlacePin: 겹침 → 북마크 + 배지 + 링', (t) async {
    await _pump(t, PlacePin(poi: _poi(), saved: true, friendCount: 3, onTap: () {}));
    expect(find.byIcon(Icons.bookmark), findsOneWidget);
    expect(find.text('3'), findsOneWidget);
    expect(find.byKey(const ValueKey('overlap-ring')), findsOneWidget);
  });
```
(`_poi()`가 없으면 기존 테스트의 Poi 생성 패턴을 따를 것. `_pump`는 MaterialApp+Scaffold+Center 래핑.)

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/goodbug/dev/odagada && flutter test test/ui/place_pin_test.dart`
Expected: FAIL(PlacePin 없음).

- [ ] **Step 3: PlacePin 구현**

`lib/ui/place_pin.dart`에 상수와 위젯 추가(기존 `_headSize`·`_NameLabel` 재사용):
```dart
const Color _friendBlue = Color(0xFF1B4F9B);

/// 저장/친구 상태를 표현하는 핀. saved=내 저장(북마크), friendCount>0=친구 수(파란 배지),
/// 둘 다=겹침(링 강조). 앵커: 머리 중심 x, 꼬리 끝(상단 46px)이 기준점.
class PlacePin extends StatelessWidget {
  final Poi poi;
  final bool saved;
  final int friendCount;
  final VoidCallback onTap;
  const PlacePin({
    super.key,
    required this.poi,
    required this.saved,
    required this.friendCount,
    required this.onTap,
  });

  @override
  Widget build(BuildContext context) {
    final c = poi.bucket;
    return GestureDetector(
      behavior: HitTestBehavior.opaque,
      onTap: onTap,
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          SizedBox(
            width: 44,
            height: _headSize,
            child: Stack(
              clipBehavior: Clip.none,
              alignment: Alignment.center,
              children: [
                // 겹침 링(오버플로) — 머리 뒤에 파란 테두리 원.
                if (saved && friendCount > 0)
                  Container(
                    key: const ValueKey('overlap-ring'),
                    width: _headSize + 8,
                    height: _headSize + 8,
                    decoration: BoxDecoration(
                      shape: BoxShape.circle,
                      border: Border.all(color: _friendBlue, width: 3),
                    ),
                  ),
                // 머리
                Container(
                  width: _headSize,
                  height: _headSize,
                  decoration: BoxDecoration(
                    color: c.color,
                    shape: BoxShape.circle,
                    border: Border.all(color: Colors.white, width: 3),
                    boxShadow: const [
                      BoxShadow(
                          color: Color(0x55000000),
                          blurRadius: 5,
                          offset: Offset(0, 2)),
                    ],
                  ),
                  child: Icon(c.icon, color: Colors.white, size: 20),
                ),
                // 내 저장 북마크(우상단)
                if (saved)
                  Positioned(
                    right: -1,
                    top: -2,
                    child: Container(
                      width: 16,
                      height: 16,
                      decoration: BoxDecoration(
                        color: Colors.white,
                        shape: BoxShape.circle,
                        border: Border.all(color: c.color, width: 1.5),
                      ),
                      child: Icon(Icons.bookmark, size: 10, color: c.color),
                    ),
                  ),
                // 친구 수 파란 배지(좌상단)
                if (friendCount > 0)
                  Positioned(
                    left: -4,
                    top: -4,
                    child: Container(
                      constraints:
                          const BoxConstraints(minWidth: 18, minHeight: 18),
                      padding: const EdgeInsets.symmetric(horizontal: 3),
                      decoration: BoxDecoration(
                        color: _friendBlue,
                        shape: BoxShape.circle,
                        border: Border.all(color: Colors.white, width: 1.5),
                      ),
                      alignment: Alignment.center,
                      child: Text('$friendCount',
                          style: const TextStyle(
                              color: Colors.white,
                              fontSize: 11,
                              fontWeight: FontWeight.bold)),
                    ),
                  ),
              ],
            ),
          ),
          Container(width: 3, height: 6, color: c.color),
          const SizedBox(height: 2),
          SizedBox(
            width: 44,
            child: LimitedBox(
              maxHeight: 30,
              child: OverflowBox(
                minWidth: 0,
                maxWidth: 120,
                alignment: Alignment.topCenter,
                child: _NameLabel(text: poi.name, bold: true),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd /Users/goodbug/dev/odagada && flutter test test/ui/place_pin_test.dart`
Expected: PASS.

- [ ] **Step 5: 커밋**
```bash
cd /Users/goodbug/dev/odagada
git add lib/ui/place_pin.dart test/ui/place_pin_test.dart
git commit -m "feat: PlacePin (3-state: bookmark / friend-count badge / overlap ring)"
```

---

### Task 4: 지도 배선 — setFriendCounts + 핀 분기

**Files:**
- Modify: `lib/ui/map_view.dart`(MapController), `lib/ui/google_map_view.dart`, `lib/ui/kakao_map_view.dart`

**Interfaces:**
- Consumes: `PlacePin`(Task3).
- Produces: `MapController.setFriendCounts(Map<String,int> byPlaceId)`.

- [ ] **Step 1: MapController에 setFriendCounts 추가**

`lib/ui/map_view.dart`의 `abstract class MapController`에 추가:
```dart
  void setFriendCounts(Map<String, int> byPlaceId);
```

- [ ] **Step 2: KakaoMapView no-op**

`_KakaoMapViewState`에 추가:
```dart
  @override
  void setFriendCounts(Map<String, int> byPlaceId) {} // 레거시 미사용
```

- [ ] **Step 3: GoogleMapView — 상태 + 핀 분기(SavedPin→PlacePin)**

`lib/ui/google_map_view.dart`:
(a) import에 `place_pin.dart`는 이미 있음. 상태 필드 추가(`_savedIds` 옆):
```dart
  Map<String, int> _friendCounts = const {};
```
(b) `setFriendCounts` 구현(`setSavedIds` 옆):
```dart
  @override
  void setFriendCounts(Map<String, int> byPlaceId) {
    if (!mounted) return;
    setState(() => _friendCounts = byPlaceId);
  }
```
(c) `_buildPins`의 분기를 교체 — 저장 or 친구>0이면 PlacePin, 아니면 CategoryDot:
```dart
      final saved = _savedIds.contains(r.poi.id);
      final fc = _friendCounts[r.poi.id] ?? 0;
      if (saved || fc > 0) {
        pins.add(Positioned(
          left: s.dx - 22,
          top: s.dy - 46,
          child: PlacePin(
            poi: r.poi,
            saved: saved,
            friendCount: fc,
            onTap: () => widget.onPinTap(r.poi),
          ),
        ));
      } else {
        pins.add(Positioned(
          left: s.dx - 15,
          top: s.dy - 15,
          child: CategoryDot(poi: r.poi, onTap: () => widget.onPinTap(r.poi)),
        ));
      }
```
(d) 기존 `SavedPin` 위젯 클래스를 `place_pin.dart`에서 **삭제**(이제 PlacePin이 대체 — 미사용). google_map_view가 SavedPin을 더 참조하지 않는지 확인.

- [ ] **Step 4: 분석 + 회귀**

Run: `cd /Users/goodbug/dev/odagada && flutter analyze && flutter test`
Expected: analyze 신규 0(미사용 SavedPin 제거 확인), 테스트 green(PlacePin 테스트가 SavedPin 테스트 대체 — 기존 SavedPin 위젯테스트는 PlacePin으로 교체됐거나 제거). 지도는 플랫폼뷰라 온디바이스 검증.

> 주의: Task3에서 SavedPin 위젯 테스트를 남겨뒀다면 SavedPin 삭제 시 그 테스트가 깨진다. 이 스텝에서 `test/ui/place_pin_test.dart`의 SavedPin 참조 테스트를 PlacePin(saved:true,friendCount:0) 케이스로 교체(또는 제거)해 green 유지.

- [ ] **Step 5: 커밋**
```bash
cd /Users/goodbug/dev/odagada
git add lib/ui/map_view.dart lib/ui/google_map_view.dart lib/ui/kakao_map_view.dart lib/ui/place_pin.dart test/ui/place_pin_test.dart
git commit -m "feat: map renders PlacePin with friend counts; setFriendCounts; drop SavedPin"
```

---

### Task 5: MapScreen 친구 오버레이 병합 + app.dart

**Files:**
- Modify: `lib/ui/map_screen.dart`, `lib/app.dart`

**Interfaces:**
- Consumes: `SocialRepository.friendSavesNear`, `friendOverlay`(Task2), `MapController.setFriendCounts`(Task4).

- [ ] **Step 1: MapScreen — socialRepo 필드 + _runSearch 병합**

`lib/ui/map_screen.dart`:
(a) import: `import '../friends/friend_save.dart';`, `import '../friends/social_repository.dart';`
(b) 필드·생성자: `final SocialRepository socialRepo;`(+ `required this.socialRepo,`)
(c) `_runSearch`를 친구 오버레이 병합으로 확장:
```dart
  Future<void> _runSearch(LatLng center, double radiusMeters) async {
    _lastCenter = center;
    _lastRadius = radiusMeters;
    final gen = ++_searchGen;
    final pois = await _searcher.search(center, radiusMeters);
    // 친구 저장(실패해도 나머지는 진행)
    List<FriendSave> friendSaves = const [];
    try {
      friendSaves = await widget.socialRepo.friendSavesNear(center, radiusMeters);
    } catch (_) {}
    if (!mounted || gen != _searchGen) return;
    final ids = pois.map((p) => p.id).toSet();
    final savedOverlay = savedPoisInViewport(_savedPlaces, center, radiusMeters)
        .where((p) => !ids.contains(p.id))
        .toList();
    final existing = {...ids, ...savedOverlay.map((p) => p.id)};
    final social = friendOverlay(friendSaves, existing);
    final recs = [...pois, ...savedOverlay, ...social.extraPins]
        .map((p) => Recommendation(poi: p, score: 0))
        .toList();
    _map?.setPins(recs);
    _map?.setSavedIds(_savedIds);
    _map?.setFriendCounts(social.friendCounts);
  }
```
(주의: 기존 `_runSearch` 내 내저장 오버레이 로직을 위와 같이 통합. `savedPoisInViewport`·`_savedPlaces`·`_savedIds`는 v2.0d 그대로.)

- [ ] **Step 2: app.dart — SupabaseSocialRepository 주입**

`lib/app.dart`:
```dart
import 'friends/social_repository.dart';
```
`MapScreen(...)`에 `socialRepo: SupabaseSocialRepository(),` 추가.

- [ ] **Step 3: 분석 + 회귀**

Run: `cd /Users/goodbug/dev/odagada && flutter analyze && flutter test`
Expected: analyze 신규 0, 테스트 green.

- [ ] **Step 4: 커밋**
```bash
cd /Users/goodbug/dev/odagada
git add lib/ui/map_screen.dart lib/app.dart
git commit -m "feat: merge friend-saved overlay + friend counts into map search"
```

---

## 온디바이스 E2E(사용자 마이그레이션 적용 후)
1. `0004_social.sql` 적용.
2. 친구(v2.1a로 연결된) 계정으로 어떤 곳을 저장 → 내 계정 지도에서 그 근처로 이동 → **파란 숫자 배지 핀** 표시.
3. 내가+친구가 같은 곳 저장 → **북마크 + 파란 배지 + 링(겹침)**.
4. 20 상한 밖 친구-저장도 뷰포트 안이면 표시.

## Self-Review 결과
- Spec 커버리지: RLS+집계(Task1)·모델/헬퍼/repo(Task2)·핀3상태(Task3)·지도배선(Task4)·병합(Task5). 통합카드=v2.1b-2 제외.
- Placeholder: 없음.
- 타입 일관성: `friendSaveFromRow`/`friendOverlay(List<FriendSave>,Set)→({extraPins,friendCounts})`/`SocialRepository.friendSavesNear(LatLng,double)`/`PlacePin(poi,saved,friendCount,onTap)`/`MapController.setFriendCounts(Map<String,int>)` — 태스크 간 일치. DB 함수 `friend_saves_near`(Task1)↔repo(Task2) 일치. SavedPin 제거(Task4)와 place_pin_test 정리 명시.
