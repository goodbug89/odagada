# v2.1c 초대 딥링크(https App Link) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 초대 링크를 https App Link(`https://goodbug89.github.io/invite/?t=<token>`)로 바꾸고, 탭하면 앱이 열려 "OO님과 친구?" 확인 → 수락까지 이어지게 한다.

**Architecture:** 링크 형식·파싱을 순수 함수(`invite_link.dart`)로 분리하고, 지금 `FriendsScreen`에 묻혀 있는 "확인 다이얼로그 → 수락"을 재사용 함수(`accept_invite_flow.dart`)로 빼서 코드입력과 딥링크가 같은 UX를 쓰게 한다. Android는 intent-filter 둘(https autoVerify + 커스텀 스킴 폴백)을 추가하고, `MapScreen`이 `app_links`로 링크를 수신한다. 도메인 검증 파일과 폴백 웹페이지는 `deploy/github-pages/`에 두고 사용자가 GitHub Pages에 올린다.

**Tech Stack:** Flutter/Dart, `app_links`, Android App Links(assetlinks.json), GitHub Pages(정적 호스팅), 기존 Supabase 친구 그래프(`FriendRepository`).

## Global Constraints

- 패키지명 `odagada`. Flutter 경로: `export PATH="/opt/homebrew/bin:$PATH"`.
- 브랜치 `feature/v2b-save`에서 작업(새 브랜치 만들지 않음).
- 링크 형식: `https://goodbug89.github.io/invite/?t=<token>` — host `goodbug89.github.io`, pathPrefix `/invite`, 토큰은 쿼리 파라미터 `t`.
- 레거시/폴백 형식: `io.supabase.odagada://invite/<token>` (토큰은 첫 path segment).
- Android applicationId: `com.odagada.app`. 디버그 서명 SHA256: `31:28:FD:06:95:7F:35:EF:EA:9F:1D:E3:8D:9C:DA:12:44:25:68:47:39:56:DC:34:17:34:DE:F0:7A:3D:F8:DC`
- `inviteTokenFromUri`는 초대가 아닌 URI(특히 Supabase OAuth 콜백 `io.supabase.odagada://login-callback/`)에 **반드시 null**을 반환해야 한다.
- 기존 baseline analyze = info 3건(saved_list_screen.dart:56, maps_loader_web.dart:2 ×2). 신규 0 유지.
- 커밋 메시지 끝: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

### Task 1: 링크 형식·파싱 (`invite_link.dart`)

**Files:**
- Create: `lib/friends/invite_link.dart`
- Modify: `lib/friends/friend_repository.dart` (하드코딩 `_linkPrefix` → `buildInviteLink`)
- Test: `test/friends/invite_link_test.dart`

**Interfaces:**
- Consumes: 없음(순수 Dart).
- Produces (Task 4가 의존):
  - `String buildInviteLink(String token)` → `https://goodbug89.github.io/invite/?t=<token>`
  - `String? inviteTokenFromUri(Uri uri)` → 초대면 토큰, 아니면 null
  - `const String inviteLinkHost = 'goodbug89.github.io';`

- [ ] **Step 1: 실패하는 테스트 작성**

```dart
// test/friends/invite_link_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:odagada/friends/invite_link.dart';

void main() {
  test('buildInviteLink는 https 초대 형식을 만든다', () {
    expect(buildInviteLink('ABCD2345'),
        'https://goodbug89.github.io/invite/?t=ABCD2345');
  });

  test('buildInviteLink → inviteTokenFromUri 왕복', () {
    final uri = Uri.parse(buildInviteLink('WXYZ6789'));
    expect(inviteTokenFromUri(uri), 'WXYZ6789');
  });

  test('https 초대 링크에서 토큰 추출', () {
    expect(
        inviteTokenFromUri(
            Uri.parse('https://goodbug89.github.io/invite/?t=ABCD2345')),
        'ABCD2345');
  });

  test('레거시 커스텀 스킴에서 토큰 추출', () {
    expect(
        inviteTokenFromUri(
            Uri.parse('io.supabase.odagada://invite/ABCD2345')),
        'ABCD2345');
  });

  test('Supabase 로그인 콜백은 초대가 아니다 → null', () {
    expect(
        inviteTokenFromUri(
            Uri.parse('io.supabase.odagada://login-callback/')),
        isNull);
  });

  test('다른 host의 /invite는 null', () {
    expect(
        inviteTokenFromUri(Uri.parse('https://example.com/invite/?t=ABCD2345')),
        isNull);
  });

  test('경로가 다르면 null', () {
    expect(
        inviteTokenFromUri(
            Uri.parse('https://goodbug89.github.io/other/?t=ABCD2345')),
        isNull);
  });

  test('t 파라미터가 없거나 비면 null', () {
    expect(inviteTokenFromUri(Uri.parse('https://goodbug89.github.io/invite/')),
        isNull);
    expect(
        inviteTokenFromUri(
            Uri.parse('https://goodbug89.github.io/invite/?t=')),
        isNull);
  });
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `export PATH="/opt/homebrew/bin:$PATH"; cd /Users/goodbug/dev/odagada && flutter test test/friends/invite_link_test.dart`
Expected: FAIL — `invite_link.dart` 없음.

- [ ] **Step 3: 구현**

```dart
// lib/friends/invite_link.dart

/// 초대 링크 형식과 파싱.
/// 기본은 https App Link(메신저에서 탭 가능·앱 없으면 안내 페이지),
/// 커스텀 스킴은 폴백 페이지의 "앱에서 열기"와 기존 공유 링크 하위호환용.
const String inviteLinkHost = 'goodbug89.github.io';
const String inviteLinkPathPrefix = '/invite';
const String inviteLegacyScheme = 'io.supabase.odagada';
const String _tokenParam = 't';

/// 공유용 초대 링크.
String buildInviteLink(String token) =>
    'https://$inviteLinkHost$inviteLinkPathPrefix/?$_tokenParam=$token';

/// URI가 초대 링크면 토큰을, 아니면 null.
/// 초대가 아닌 URI(로그인 콜백 등)에는 반드시 null을 돌려준다.
String? inviteTokenFromUri(Uri uri) {
  if (uri.scheme == 'https' &&
      uri.host == inviteLinkHost &&
      uri.path.startsWith(inviteLinkPathPrefix)) {
    final t = uri.queryParameters[_tokenParam];
    return (t == null || t.isEmpty) ? null : t;
  }
  if (uri.scheme == inviteLegacyScheme && uri.host == 'invite') {
    final segs = uri.pathSegments.where((s) => s.isNotEmpty).toList();
    return segs.isEmpty ? null : segs.first;
  }
  return null;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `export PATH="/opt/homebrew/bin:$PATH"; cd /Users/goodbug/dev/odagada && flutter test test/friends/invite_link_test.dart`
Expected: PASS (8 tests).

- [ ] **Step 5: `friend_repository`가 `buildInviteLink`를 쓰도록 변경**

`lib/friends/friend_repository.dart`에서 import를 추가:
```dart
import 'invite_link.dart';
```
이 줄을 삭제:
```dart
  static const _linkPrefix = 'io.supabase.odagada://invite/';
```
그리고 이 줄
```dart
    return (token: token, link: '$_linkPrefix$token');
```
을 다음으로 교체:
```dart
    return (token: token, link: buildInviteLink(token));
```

- [ ] **Step 6: 전체 테스트 + analyze**

Run: `export PATH="/opt/homebrew/bin:$PATH"; cd /Users/goodbug/dev/odagada && flutter test && flutter analyze`
Expected: 전체 통과, analyze 신규 0(기존 3). `_linkPrefix` 미사용 경고 없음(삭제했으므로).

- [ ] **Step 7: Commit**

```bash
cd /Users/goodbug/dev/odagada && git add lib/friends/invite_link.dart lib/friends/friend_repository.dart test/friends/invite_link_test.dart && git commit -m "feat(invite): https App Link format + URI parsing

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 공유 수락 플로우 (`accept_invite_flow.dart`) + FriendsScreen 리팩터

**Files:**
- Create: `lib/ui/accept_invite_flow.dart`
- Modify: `lib/ui/friends_screen.dart` (`_addByCode`가 공유 함수 호출)
- Test: `test/ui/accept_invite_flow_test.dart`

**Interfaces:**
- Consumes: `FriendRepository`(`lib/friends/friend_repository.dart`) — `Future<InviteInfo> inviteInfo(String token)`, `Future<void> acceptInvite(String token)`. `InviteInfo`(`lib/friends/friend.dart`) — `.inviterName`(String), `.valid`(bool).
- Produces (Task 4가 의존):
  - `Future<bool> runAcceptInviteFlow(BuildContext context, FriendRepository repo, String token)` — 수락되면 true.

- [ ] **Step 1: 실패하는 위젯 테스트 작성**

```dart
// test/ui/accept_invite_flow_test.dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:odagada/friends/friend.dart';
import 'package:odagada/friends/friend_repository.dart';
import 'package:odagada/ui/accept_invite_flow.dart';

class _FakeRepo implements FriendRepository {
  final InviteInfo info;
  int acceptCalls = 0;
  _FakeRepo({this.info = const InviteInfo(inviterName: '영희', valid: true)});
  @override
  Future<({String token, String link})> createInvite() async =>
      (token: 'T', link: 'L');
  @override
  Future<InviteInfo> inviteInfo(String token) async => info;
  @override
  Future<void> acceptInvite(String token) async => acceptCalls++;
  @override
  Future<List<Friend>> listFriends() async => const [];
  @override
  Future<void> removeFriend(String otherId) async {}
}

/// 버튼을 눌러 플로우를 실행하고 결과를 담아둔다.
Future<void> _pumpRunner(
    WidgetTester t, FriendRepository repo, List<bool> out) async {
  await t.pumpWidget(MaterialApp(
    home: Scaffold(
      body: Builder(
        builder: (ctx) => ElevatedButton(
          onPressed: () async {
            out.add(await runAcceptInviteFlow(ctx, repo, 'ABCD2345'));
          },
          child: const Text('go'),
        ),
      ),
    ),
  ));
  await t.tap(find.text('go'));
  await t.pumpAndSettle();
}

void main() {
  testWidgets('유효한 초대 → 확인 다이얼로그 → 수락 시 acceptInvite 호출·true', (t) async {
    final repo = _FakeRepo();
    final out = <bool>[];
    await _pumpRunner(t, repo, out);
    expect(find.textContaining('영희'), findsOneWidget); // 확인 다이얼로그
    await t.tap(find.text('수락'));
    await t.pumpAndSettle();
    expect(repo.acceptCalls, 1);
    expect(out.single, isTrue);
  });

  testWidgets('취소하면 수락하지 않고 false', (t) async {
    final repo = _FakeRepo();
    final out = <bool>[];
    await _pumpRunner(t, repo, out);
    await t.tap(find.text('취소'));
    await t.pumpAndSettle();
    expect(repo.acceptCalls, 0);
    expect(out.single, isFalse);
  });

  testWidgets('무효한 초대 → 다이얼로그 없이 false + 안내', (t) async {
    final repo = _FakeRepo(info: const InviteInfo(inviterName: '', valid: false));
    final out = <bool>[];
    await _pumpRunner(t, repo, out);
    expect(find.text('수락'), findsNothing);
    expect(repo.acceptCalls, 0);
    expect(out.single, isFalse);
    expect(find.text('유효하지 않은 초대예요.'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `export PATH="/opt/homebrew/bin:$PATH"; cd /Users/goodbug/dev/odagada && flutter test test/ui/accept_invite_flow_test.dart`
Expected: FAIL — `accept_invite_flow.dart` 없음.

- [ ] **Step 3: 공유 플로우 구현**

```dart
// lib/ui/accept_invite_flow.dart
import 'package:flutter/material.dart';
import '../friends/friend.dart';
import '../friends/friend_repository.dart';

/// 초대 토큰을 확인하고 사용자가 승인하면 수락한다. 수락되면 true.
/// 코드 입력(FriendsScreen)과 딥링크 수신(MapScreen)이 같은 UX를 쓰도록 공유한다.
Future<bool> runAcceptInviteFlow(
    BuildContext context, FriendRepository repo, String token) async {
  void toast(String msg) {
    if (!context.mounted) return;
    ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(msg)));
  }

  final InviteInfo info;
  try {
    info = await repo.inviteInfo(token);
  } catch (_) {
    toast('초대 확인에 실패했어요.');
    return false;
  }
  if (!info.valid) {
    toast('유효하지 않은 초대예요.');
    return false;
  }
  if (!context.mounted) return false;
  final ok = await showDialog<bool>(
    context: context,
    builder: (ctx) => AlertDialog(
      content: Text('${info.inviterName}님과 친구를 맺을까요?'),
      actions: [
        TextButton(
            onPressed: () => Navigator.pop(ctx, false),
            child: const Text('취소')),
        FilledButton(
            onPressed: () => Navigator.pop(ctx, true),
            child: const Text('수락')),
      ],
    ),
  );
  if (ok != true) return false;
  try {
    await repo.acceptInvite(token);
  } catch (_) {
    toast('이미 친구이거나 만료된 초대예요.');
    return false;
  }
  toast('친구가 됐어요!');
  return true;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `export PATH="/opt/homebrew/bin:$PATH"; cd /Users/goodbug/dev/odagada && flutter test test/ui/accept_invite_flow_test.dart`
Expected: PASS (3 tests).

- [ ] **Step 5: `FriendsScreen._addByCode`를 공유 플로우로 교체**

`lib/ui/friends_screen.dart` 상단 import에 추가:
```dart
import 'accept_invite_flow.dart';
```
그리고 `_addByCode` 메서드 전체(아래)
```dart
  Future<void> _addByCode() async {
    final token = _codeCtrl.text.trim();
    if (token.isEmpty) return;
    late final InviteInfo info;
    try {
      info = await widget.repo.inviteInfo(token);
    } catch (_) {
      _toast('초대 확인에 실패했어요.');
      return;
    }
    if (!info.valid) {
      _toast('유효하지 않은 초대예요.');
      return;
    }
    if (!mounted) return;
    final ok = await showDialog<bool>(
      context: context,
      builder: (ctx) => AlertDialog(
        content: Text('${info.inviterName}님과 친구를 맺을까요?'),
        actions: [
          TextButton(
              onPressed: () => Navigator.pop(ctx, false),
              child: const Text('취소')),
          FilledButton(
              onPressed: () => Navigator.pop(ctx, true),
              child: const Text('수락')),
        ],
      ),
    );
    if (ok != true) return;
    try {
      await widget.repo.acceptInvite(token);
    } catch (_) {
      _toast('이미 친구이거나 만료된 초대예요.');
      return;
    }
    if (!mounted) return;
    _codeCtrl.clear();
    await _reload();
    _toast('친구가 됐어요!');
  }
```
를 다음으로 교체:
```dart
  Future<void> _addByCode() async {
    final token = _codeCtrl.text.trim();
    if (token.isEmpty) return;
    final ok = await runAcceptInviteFlow(context, widget.repo, token);
    if (!ok || !mounted) return;
    _codeCtrl.clear();
    await _reload();
  }
```
(성공/실패 토스트는 이제 공유 플로우가 낸다. `Friend`/`InviteInfo` import가 여전히 쓰이는지 확인하고, 안 쓰이면 제거해 analyze 경고를 피한다 — `_friends` 목록이 `Friend`를 쓰므로 `friend.dart` import는 유지된다.)

- [ ] **Step 6: 전체 테스트 + analyze (기존 FriendsScreen 테스트 회귀 확인)**

Run: `export PATH="/opt/homebrew/bin:$PATH"; cd /Users/goodbug/dev/odagada && flutter test && flutter analyze`
Expected: 전체 통과 — 특히 기존 `test/ui/friends_screen_test.dart`의 "유효하지 않은 초대 코드 → 토스트", "유효한 초대 코드 → 확인 다이얼로그 → 수락 → 목록 갱신"이 그대로 통과해야 한다(문구·동작 동일). analyze 신규 0.

- [ ] **Step 7: Commit**

```bash
cd /Users/goodbug/dev/odagada && git add lib/ui/accept_invite_flow.dart lib/ui/friends_screen.dart test/ui/accept_invite_flow_test.dart && git commit -m "refactor(invite): extract shared accept-invite flow

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: GitHub Pages 배포 자산

**Files:**
- Create: `deploy/github-pages/.well-known/assetlinks.json`
- Create: `deploy/github-pages/.nojekyll`
- Create: `deploy/github-pages/invite/index.html`
- Create: `deploy/github-pages/README.md`

**Interfaces:**
- Consumes: 없음(정적 파일). 링크 형식은 Task 1의 `buildInviteLink`와 일치해야 한다(`/invite/?t=<token>`).
- Produces: 사용자가 `goodbug89.github.io` 저장소에 올릴 파일들. 앱 코드가 참조하지 않는다.

단위 테스트가 없는 태스크다. 검증: JSON이 유효하게 파싱되고, 4개 파일이 생성되며, `flutter analyze`/`flutter test`가 영향받지 않음(다트 코드 아님).

- [ ] **Step 1: `assetlinks.json` 작성**

`deploy/github-pages/.well-known/assetlinks.json`:
```json
[
  {
    "relation": ["delegate_permission/common.handle_all_urls"],
    "target": {
      "namespace": "android_app",
      "package_name": "com.odagada.app",
      "sha256_cert_fingerprints": [
        "31:28:FD:06:95:7F:35:EF:EA:9F:1D:E3:8D:9C:DA:12:44:25:68:47:39:56:DC:34:17:34:DE:F0:7A:3D:F8:DC"
      ]
    }
  }
]
```

- [ ] **Step 2: `.nojekyll` 작성(빈 파일)**

```bash
cd /Users/goodbug/dev/odagada && mkdir -p deploy/github-pages && touch deploy/github-pages/.nojekyll
```
(Jekyll이 `.`으로 시작하는 `.well-known/`을 숨기지 않도록 하기 위함.)

- [ ] **Step 3: 폴백 페이지 작성**

`deploy/github-pages/invite/index.html`:
```html
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>오다가다 친구 초대</title>
<style>
  body { font-family: -apple-system, BlinkMacSystemFont, "Apple SD Gothic Neo", sans-serif;
         margin: 0; padding: 32px 20px; background: #FFF6EF; color: #222; }
  .wrap { max-width: 420px; margin: 0 auto; text-align: center; }
  h1 { font-size: 22px; margin: 0 0 8px; }
  p { color: #555; line-height: 1.6; }
  .code { font-size: 30px; font-weight: 800; letter-spacing: 4px;
          background: #fff; border: 2px dashed #FF7A2F; border-radius: 12px;
          padding: 16px; margin: 20px 0; word-break: break-all; }
  .btn { display: inline-block; background: #FF7A2F; color: #fff; text-decoration: none;
         padding: 14px 24px; border-radius: 999px; font-weight: 700; }
  .hint { font-size: 13px; color: #888; }
</style>
</head>
<body>
<div class="wrap">
  <h1>오다가다 친구 초대</h1>
  <p>앱이 설치돼 있으면 자동으로 열려요.<br>안 열리면 아래 코드를 앱에 입력하세요.</p>
  <div class="code" id="code">-</div>
  <p><a class="btn" id="open" href="#">앱에서 열기</a></p>
  <p class="hint">앱 → 계정 메뉴 → 친구 → “친구 추가”에 코드 입력</p>
</div>
<script>
  var t = new URLSearchParams(location.search).get('t') || '';
  document.getElementById('code').textContent = t || '코드 없음';
  document.getElementById('open').href =
      'io.supabase.odagada://invite/' + encodeURIComponent(t);
</script>
</body>
</html>
```

- [ ] **Step 4: 사용자 배포 안내 작성**

`deploy/github-pages/README.md`:
```markdown
# GitHub Pages 배포 (초대 딥링크용)

Android App Links는 `assetlinks.json`이 **도메인 루트**에 있어야 하므로,
사용자 Pages 저장소(`goodbug89.github.io`)에 아래 내용을 그대로 올린다.

## 절차
1. GitHub에 **공개 저장소 `goodbug89.github.io`** 생성(이미 있으면 재사용).
2. 이 폴더의 파일을 저장소 루트에 그대로 복사:
   - `.well-known/assetlinks.json`
   - `.nojekyll`
   - `invite/index.html`
3. push 후 Settings → Pages에서 Source를 `main` 브랜치 루트로 설정.
4. 확인:
   - `https://goodbug89.github.io/.well-known/assetlinks.json` 이 JSON을 그대로 반환하는지
   - `https://goodbug89.github.io/invite/?t=TEST1234` 페이지에 코드 `TEST1234`가 보이는지

## 서명 지문
`assetlinks.json`의 지문은 **디버그 키스토어**(`~/.android/debug.keystore`) SHA256이다.
릴리스 서명으로 배포할 때는 릴리스 지문을 배열에 **추가**한다(둘 다 나열 가능):

    keytool -list -v -keystore <릴리스 키스토어> -alias <별칭>

## 검증 상태 확인(기기)
    adb shell pm get-app-links com.odagada.app
    adb shell pm verify-app-links --re-verify com.odagada.app
```

- [ ] **Step 5: JSON 유효성 + 파일 확인**

Run:
```bash
cd /Users/goodbug/dev/odagada && python3 -m json.tool deploy/github-pages/.well-known/assetlinks.json > /dev/null && echo "JSON OK" && ls -a deploy/github-pages deploy/github-pages/.well-known deploy/github-pages/invite
```
Expected: `JSON OK` 출력 + `.nojekyll`, `.well-known/assetlinks.json`, `invite/index.html`, `README.md` 존재.

- [ ] **Step 6: Commit**

```bash
cd /Users/goodbug/dev/odagada && git add deploy/github-pages && git commit -m "chore(deploy): GitHub Pages assets for invite App Links

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```
커밋 전에 `git status --short deploy/github-pages`로 **4개 파일이 모두** 스테이징됐는지 확인한다(`.nojekyll`·`.well-known/assetlinks.json` 포함 — .gitignore에 걸리지 않음은 확인됨).

---

### Task 4: 딥링크 수신 배선 (manifest + `app_links` + MapScreen)

**Files:**
- Modify: `pubspec.yaml` (`app_links` 추가)
- Modify: `android/app/src/main/AndroidManifest.xml` (intent-filter 2개 추가)
- Modify: `lib/ui/map_screen.dart` (링크 수신 + 보류 토큰 + auth 리스너)

**Interfaces:**
- Consumes: `inviteTokenFromUri(Uri) → String?` (Task 1), `runAcceptInviteFlow(BuildContext, FriendRepository, String) → Future<bool>` (Task 2), 기존 `widget.auth`(`AuthController`, `ChangeNotifier`, `.user`/`.signInWithGoogle`), `widget.friendRepo`, `showLoginSheet(context, onGoogle:)`.
- Produces: (앱 배선; 다른 태스크가 소비하지 않음.)

플랫폼(딥링크)이라 새 단위 테스트가 없다. 검증: `flutter analyze` 신규 0 + 전체 `flutter test` 통과 + 컴파일. 실제 수신은 온디바이스.

- [ ] **Step 1: `app_links` 의존성 추가 후 API 확인**

Run:
```bash
export PATH="/opt/homebrew/bin:$PATH"; cd /Users/goodbug/dev/odagada && flutter pub add app_links && grep -n "Future<Uri?> getInitial\|Stream<Uri> get uri" $(find ~/.pub-cache/hosted -maxdepth 3 -type d -name "app_links-*" | head -1)/lib/src/app_links.dart
```
Expected: `app_links`가 pubspec에 추가되고, 위 grep이 초기 링크 1회 조회 메서드와 링크 스트림 게터의 실제 이름을 출력한다.
**Step 3의 코드는 `getInitialLink()` + `uriLinkStream`을 쓴다. grep 결과 이름이 다르면(예: `getInitialAppLink()`) 설치된 API 이름으로 바꿔 쓴다.** 계약은 동일하다: 앱 시작 시 최초 링크 1회 + 이후 링크 스트림.

- [ ] **Step 2: AndroidManifest에 intent-filter 2개 추가**

`android/app/src/main/AndroidManifest.xml`의 `<activity android:name=".MainActivity" ...>` 안, 기존 `login-callback` intent-filter **바로 다음**에 추가(기존 LAUNCHER·login-callback 필터는 건드리지 않는다):
```xml
            <!-- https App Link: 메신저에서 탭 가능, autoVerify로 앱이 바로 열림 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="goodbug89.github.io"
                      android:pathPrefix="/invite"/>
            </intent-filter>
            <!-- 커스텀 스킴: 폴백 페이지의 "앱에서 열기" + 기존 공유 링크 하위호환 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="io.supabase.odagada" android:host="invite"/>
            </intent-filter>
```

- [ ] **Step 3: MapScreen에 링크 수신 배선**

`lib/ui/map_screen.dart` 상단 import에 추가:
```dart
import 'package:app_links/app_links.dart';
import '../friends/invite_link.dart';
import 'accept_invite_flow.dart';
```

`_MapScreenState`의 필드 영역(`int _searchGen = 0;` 아래)에 추가:
```dart
  StreamSubscription<Uri>? _linkSub;
  String? _pendingInviteToken; // 미로그인 상태로 받은 초대(로그인 후 이어서 수락)
```

`initState`를 다음으로 교체:
```dart
  @override
  void initState() {
    super.initState();
    _start();
    _initDeepLinks();
    widget.auth.addListener(_onAuthChanged);
  }
```

`initState` 아래에 다음 메서드들을 추가:
```dart
  /// 앱 시작 시 최초 링크 + 실행 중 들어오는 링크를 함께 구독한다.
  Future<void> _initDeepLinks() async {
    final links = AppLinks();
    try {
      final initial = await links.getInitialLink();
      if (initial != null) _handleUri(initial);
    } catch (_) {
      // 최초 링크 조회 실패는 무시(딥링크 없이 실행된 경우 포함)
    }
    _linkSub = links.uriLinkStream.listen(_handleUri, onError: (_) {});
  }

  /// 초대 링크만 처리한다. 로그인 콜백 등 다른 URI는 무시.
  void _handleUri(Uri uri) {
    final token = inviteTokenFromUri(uri);
    if (token == null) return;
    if (widget.auth.user == null) {
      _pendingInviteToken = token; // 로그인되면 _onAuthChanged가 이어서 처리
      showLoginSheet(context, onGoogle: widget.auth.signInWithGoogle);
      return;
    }
    _acceptInvite(token);
  }

  /// 로그인이 완료되면 보류해 둔 초대를 이어서 수락한다.
  void _onAuthChanged() {
    final token = _pendingInviteToken;
    if (token == null || widget.auth.user == null) return;
    _pendingInviteToken = null;
    _acceptInvite(token);
  }

  Future<void> _acceptInvite(String token) async {
    if (!mounted) return;
    await runAcceptInviteFlow(context, widget.friendRepo, token);
  }
```

`dispose`를 다음으로 교체:
```dart
  @override
  void dispose() {
    _idleDebounce?.cancel();
    _sub?.cancel();
    _linkSub?.cancel();
    widget.auth.removeListener(_onAuthChanged);
    super.dispose();
  }
```

- [ ] **Step 4: analyze + 전체 테스트**

Run: `export PATH="/opt/homebrew/bin:$PATH"; cd /Users/goodbug/dev/odagada && flutter analyze && flutter test`
Expected: analyze 신규 0(기존 3). 전체 테스트 통과. `AppLinks`/`inviteTokenFromUri`/`runAcceptInviteFlow` 미정의 오류 없음.

- [ ] **Step 5: Commit**

```bash
cd /Users/goodbug/dev/odagada && git add pubspec.yaml pubspec.lock android/app/src/main/AndroidManifest.xml lib/ui/map_screen.dart && git commit -m "feat(invite): receive invite deep links and accept in-app

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 온디바이스 검증 (전체 태스크 후, 사용자와 함께)

사용자가 `deploy/github-pages/README.md`대로 GitHub Pages를 올린 뒤:

1. **앱 경로(검증 무관)**: `adb shell am start -a android.intent.action.VIEW -d "https://goodbug89.github.io/invite/?t=<실제토큰>"` → 앱이 열리고 "OO님과 친구?" 다이얼로그 → 수락 → 친구 목록에 반영.
2. **커스텀 스킴 폴백**: `adb shell am start -a android.intent.action.VIEW -d "io.supabase.odagada://invite/<토큰>"` → 같은 다이얼로그.
3. **로그인 콜백 무시 회귀**: 로그아웃 후 구글 로그인 → OAuth 콜백(`io.supabase.odagada://login-callback/`)이 초대로 오인되지 않고 로그인만 정상 완료.
4. **미로그인 → 로그인 이어받기**: 로그아웃 상태에서 1번 실행 → 로그인 시트 → 로그인 완료 후 초대 다이얼로그가 이어서 뜨는지.
5. **App Links 검증 상태**: `adb shell pm get-app-links com.odagada.app` → 도메인이 `verified`인지(아니면 `pm verify-app-links --re-verify com.odagada.app` 후 재확인). 검증되면 카카오톡 등에서 링크 탭 시 앱이 바로 열린다.
6. 실제 메신저에서 링크 탭 → 앱 열림(1~5 통과 후).

## Self-Review 메모(스펙 대비 커버리지)

- 스펙 §2 링크 형식 → Task 1(`buildInviteLink`) + Task 3(폴백 페이지 쿼리 파싱). §3 호스팅 3파일 → Task 3. §4 intent-filter 2개 → Task 4 Step2. §5.1 파싱·repo 교체 → Task 1. §5.2 공유 수락 플로우 → Task 2. §5.3 수신·미로그인 보류·auth 리스너·dispose → Task 4 Step3. §6 테스트 → Task 1(8) + Task 2(3) + 기존 회귀(Task 2 Step6) + 온디바이스. §7 엣지(미배포 검증 실패·재검증 명령) → README(Task 3) + 온디바이스 5번.
- 스펙 §5.3의 "미로그인 자동 이어받기"는 `AuthController`가 이미 `ChangeNotifier`이므로 auth 리스너로 구현(Task 4 Step3).
- iOS Universal Links·Branch deferred·QR(§8) = 범위 밖.
