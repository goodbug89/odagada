# 오다가다 v2.0b — 맛집 저장(핵심) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 로그인한 사용자가 지도 핀을 탭해 **맛집을 저장**하고, **내 저장 목록**에서 보고 삭제할 수 있게 한다. (검색 저장 · 직접 추가(long-press) · 7종 카테고리 버킷 · 지도 저장 레이어는 후속 계획.)

**Architecture:** v2.0a의 Supabase/Auth 토대 위에 얹는다. `saved_places` 테이블(+RLS)에 저장하고, 얇은 `SavedPlaceRepository`가 Supabase CRUD를 감싼다. Row→모델 매핑은 순수 함수로 분리해 단위 테스트한다. 저장은 로그인 필요 — 익명이면 InfoCard의 "저장" 탭 시 로그인 시트를 띄운다.

**Tech Stack:** Flutter, `supabase_flutter`(기존), 기존 `AuthController`/`InfoCard`/`MapScreen`.

## Global Constraints

- 설계 문서: `docs/superpowers/specs/2026-07-14-social-map-v2-design.md`. 이 계획은 v2.0의 "저장(핵심)" 슬라이스.
- 전제: v2.0a 완료(로그인/프로필). 브랜치 `feature/v2b-save`가 v2a 위에 스택됨.
- 저장 = 단일 "보증" 액션 + 선택 한줄 메모. 저장은 **친구에게 공개가 기본**(`is_public=true`).
- 저장 항목 두 형태: Places 연결형(`place_id` = Google place id, 즉 `Poi.id`) / 직접추가형(후속).
- **저장은 로그인 필요** — 익명이면 로그인 유도(첫 실행 강제는 아님, 저장 시점).
- RLS: 본인 저장만 CRUD (친구 조회는 v2.1).
- 카테고리는 이번 계획에선 `Poi.category`(구글 표시 텍스트) 그대로 저장. **7종 버킷 매핑은 후속**.
- 순수 매핑은 `flutter test` 단위 테스트. `SavedPlaceRepository`는 Supabase 의존 얇은 래퍼라 단위테스트 없이 Task 4 E2E로 검증(v2a `AuthController`와 동일 방침).
- 기존 테스트(31개) 회귀 없어야. 패키지명 `odagada`.
- Supabase 프로젝트 ref: `drfkumzrynejpctlpmqx` (키는 `.env`, 커밋 금지).

---

## File Structure

```
lib/
  saved/
    saved_place.dart            # SavedPlace 모델 + savedPlaceFromRow(map) 순수 매핑 + toInsert
    saved_place_repository.dart # Supabase CRUD 래퍼(save/deleteByPlaceId/listMine/savedPlaceIds)
  ui/
    info_card.dart              # (수정) onSave 콜백 + isSaved 상태 + "저장/저장됨" 버튼
    map_screen.dart             # (수정) 저장 배선 + auth gate + 내 저장 id 로드
    saved_list_screen.dart      # 내 저장 목록 화면(리스트 + 삭제)
supabase/
  migrations/0002_saved_places.sql
test/
  saved/saved_place_test.dart   # savedPlaceFromRow / toInsert 단위 테스트
  ui/info_card_test.dart        # (수정) 저장 버튼 동작 테스트 추가
```

---

## Task 0: saved_places 스키마 + RLS

**Files:**
- Create: `supabase/migrations/0002_saved_places.sql`

**Interfaces:**
- Produces: `public.saved_places(id, owner_id, place_id, name, lat, lng, category, memo, is_public, created_at)` + RLS(본인만).

- [ ] **Step 1: 마이그레이션 SQL 작성**

Create `supabase/migrations/0002_saved_places.sql`:
```sql
create table if not exists public.saved_places (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references public.profiles(id) on delete cascade,
  place_id text,                       -- Google place id (연결형). NULL이면 직접추가형(후속)
  name text not null,
  lat double precision not null,
  lng double precision not null,
  category text,
  memo text,
  is_public boolean not null default true,
  created_at timestamptz not null default now()
);

-- 같은 사용자가 같은 장소를 중복 저장하지 않도록(연결형 한정)
create unique index if not exists saved_places_owner_place_uniq
  on public.saved_places(owner_id, place_id) where place_id is not null;

create index if not exists saved_places_owner_idx on public.saved_places(owner_id);

alter table public.saved_places enable row level security;

create policy "saved_select_own" on public.saved_places
  for select using (auth.uid() = owner_id);
create policy "saved_insert_own" on public.saved_places
  for insert with check (auth.uid() = owner_id);
create policy "saved_delete_own" on public.saved_places
  for delete using (auth.uid() = owner_id);
create policy "saved_update_own" on public.saved_places
  for update using (auth.uid() = owner_id);
```

- [ ] **Step 2: Supabase에 적용 (사용자)**

Supabase 대시보드 → **SQL Editor** → 위 내용 붙여넣고 **Run**. Expected: Success. **Table Editor**에 `saved_places` 보임.

- [ ] **Step 3: 커밋**

```bash
git add supabase/migrations/0002_saved_places.sql
git commit -m "feat(db): saved_places 테이블 + RLS (v2.0b Task 0)"
```

---

## Task 1: SavedPlace 모델 + 순수 매핑

**Files:**
- Create: `lib/saved/saved_place.dart`
- Test: `test/saved/saved_place_test.dart`

**Interfaces:**
- Produces:
  - `class SavedPlace { final String id; final String? placeId; final String name; final double lat; final double lng; final String? category; final String? memo; const SavedPlace({required id, placeId, required name, required lat, required lng, category, memo}); }`
  - `SavedPlace savedPlaceFromRow(Map<String,dynamic> row)` — Supabase row → 모델.
  - `Map<String,dynamic> savedPlaceInsert({required String ownerId, required String? placeId, required String name, required double lat, required double lng, String? category, String? memo})` — insert용 map(is_public 생략 시 DB 기본 true).

- [ ] **Step 1: 실패 테스트 작성**

Create `test/saved/saved_place_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:odagada/saved/saved_place.dart';

void main() {
  test('Supabase row를 SavedPlace로 매핑', () {
    final sp = savedPlaceFromRow({
      'id': 'row-1',
      'place_id': 'g-123',
      'name': '소문난 국밥',
      'lat': 37.5665,
      'lng': 126.9780,
      'category': '음식점',
      'memo': '점심 웨이팅 없음',
    });
    expect(sp.id, 'row-1');
    expect(sp.placeId, 'g-123');
    expect(sp.name, '소문난 국밥');
    expect(sp.lat, closeTo(37.5665, 1e-9));
    expect(sp.lng, closeTo(126.9780, 1e-9));
    expect(sp.category, '음식점');
    expect(sp.memo, '점심 웨이팅 없음');
  });

  test('lat/lng가 int로 와도 double로 매핑', () {
    final sp = savedPlaceFromRow({
      'id': 'r', 'place_id': null, 'name': 'X', 'lat': 37, 'lng': 127,
    });
    expect(sp.lat, 37.0);
    expect(sp.lng, 127.0);
    expect(sp.placeId, isNull);
    expect(sp.memo, isNull);
  });

  test('insert map은 owner_id/place_id/name/lat/lng/category/memo를 담는다', () {
    final m = savedPlaceInsert(
      ownerId: 'u-1', placeId: 'g-1', name: '집', lat: 1.0, lng: 2.0,
      category: '카페', memo: null,
    );
    expect(m['owner_id'], 'u-1');
    expect(m['place_id'], 'g-1');
    expect(m['name'], '집');
    expect(m['lat'], 1.0);
    expect(m['lng'], 2.0);
    expect(m['category'], '카페');
    expect(m.containsKey('memo'), isTrue);
  });
}
```

- [ ] **Step 2: 실패 확인**

Run: `flutter test test/saved/saved_place_test.dart`
Expected: FAIL — `saved_place.dart` 미존재.

- [ ] **Step 3: 구현 작성**

Create `lib/saved/saved_place.dart`:
```dart
/// 저장된 장소(내 저장). Supabase saved_places 행과 1:1.
class SavedPlace {
  final String id;
  final String? placeId; // Google place id (연결형). null이면 직접추가형.
  final String name;
  final double lat;
  final double lng;
  final String? category;
  final String? memo;

  const SavedPlace({
    required this.id,
    this.placeId,
    required this.name,
    required this.lat,
    required this.lng,
    this.category,
    this.memo,
  });
}

double _toDouble(Object? v) => (v as num).toDouble();

/// Supabase row(map) → SavedPlace.
SavedPlace savedPlaceFromRow(Map<String, dynamic> row) {
  return SavedPlace(
    id: row['id'] as String,
    placeId: row['place_id'] as String?,
    name: row['name'] as String,
    lat: _toDouble(row['lat']),
    lng: _toDouble(row['lng']),
    category: row['category'] as String?,
    memo: row['memo'] as String?,
  );
}

/// insert용 map. is_public은 생략 → DB 기본값(true).
Map<String, dynamic> savedPlaceInsert({
  required String ownerId,
  required String? placeId,
  required String name,
  required double lat,
  required double lng,
  String? category,
  String? memo,
}) {
  return {
    'owner_id': ownerId,
    'place_id': placeId,
    'name': name,
    'lat': lat,
    'lng': lng,
    'category': category,
    'memo': memo,
  };
}
```

- [ ] **Step 4: 통과 확인**

Run: `flutter test test/saved/saved_place_test.dart`
Expected: PASS (3개).

- [ ] **Step 5: 커밋**

```bash
git add lib/saved/saved_place.dart test/saved/saved_place_test.dart
git commit -m "feat(saved): SavedPlace 모델 + 순수 매핑"
```

---

## Task 2: SavedPlaceRepository (Supabase CRUD)

**Files:**
- Create: `lib/saved/saved_place_repository.dart`

**Interfaces:**
- Consumes: `SavedPlace`, `savedPlaceFromRow`, `savedPlaceInsert`, `Supabase.instance.client`.
- Produces:
  - `class SavedPlaceRepository { SavedPlaceRepository([SupabaseClient?]); Future<void> save({required String ownerId, required Poi poi, String? memo}); Future<void> deleteByPlaceId({required String ownerId, required String placeId}); Future<List<SavedPlace>> listMine(String ownerId); Future<Set<String>> savedPlaceIds(String ownerId); }`

> 참고: Supabase 의존 얇은 래퍼라 단위 테스트 대신 Task 4 E2E로 검증(순수 매핑은 Task 1에서 테스트됨). 의도된 설계.

- [ ] **Step 1: 구현 작성**

Create `lib/saved/saved_place_repository.dart`:
```dart
import 'package:supabase_flutter/supabase_flutter.dart';
import '../core/models/poi.dart';
import 'saved_place.dart';

/// saved_places CRUD를 감싸는 얇은 저장소.
class SavedPlaceRepository {
  final SupabaseClient _client;
  SavedPlaceRepository([SupabaseClient? client])
      : _client = client ?? Supabase.instance.client;

  static const _table = 'saved_places';

  /// Places 연결형 저장(중복이면 무시). place_id = Poi.id.
  Future<void> save({
    required String ownerId,
    required Poi poi,
    String? memo,
  }) async {
    await _client.from(_table).upsert(
      savedPlaceInsert(
        ownerId: ownerId,
        placeId: poi.id,
        name: poi.name,
        lat: poi.position.lat,
        lng: poi.position.lng,
        category: poi.category,
        memo: memo,
      ),
      onConflict: 'owner_id,place_id',
    );
  }

  Future<void> deleteByPlaceId({
    required String ownerId,
    required String placeId,
  }) async {
    await _client
        .from(_table)
        .delete()
        .eq('owner_id', ownerId)
        .eq('place_id', placeId);
  }

  /// 내 저장 목록(최신순).
  Future<List<SavedPlace>> listMine(String ownerId) async {
    final rows = await _client
        .from(_table)
        .select()
        .eq('owner_id', ownerId)
        .order('created_at', ascending: false);
    return (rows as List)
        .map((r) => savedPlaceFromRow(r as Map<String, dynamic>))
        .toList();
  }

  /// 내가 저장한 place_id 집합(핀 '저장됨' 표시용).
  Future<Set<String>> savedPlaceIds(String ownerId) async {
    final rows = await _client
        .from(_table)
        .select('place_id')
        .eq('owner_id', ownerId)
        .not('place_id', 'is', null);
    return (rows as List)
        .map((r) => (r as Map<String, dynamic>)['place_id'] as String)
        .toSet();
  }
}
```

- [ ] **Step 2: 컴파일 확인**

Run: `flutter analyze lib/saved/saved_place_repository.dart`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add lib/saved/saved_place_repository.dart
git commit -m "feat(saved): SavedPlaceRepository — Supabase CRUD"
```

---

## Task 3: InfoCard 저장 버튼 + MapScreen 배선(auth gate)

**Files:**
- Modify: `lib/ui/info_card.dart`
- Modify: `lib/ui/map_screen.dart`
- Test: `test/ui/info_card_test.dart`

**Interfaces:**
- Consumes: `AuthController`, `SavedPlaceRepository`, `showLoginSheet`, `Poi`.
- Produces: InfoCard에 `bool isSaved` + `VoidCallback onSave` 추가. MapScreen이 저장/해제 배선.

- [ ] **Step 1: InfoCard 테스트 추가(실패)**

`test/ui/info_card_test.dart`에 테스트 추가(기존 테스트 유지). 파일 상단 import는 그대로 두고, `main()` 안에 아래 test를 추가:
```dart
  testWidgets('저장 버튼 탭 시 onSave 호출, isSaved면 "저장됨" 표시', (tester) async {
    var saved = false;
    final poi = Poi(
      id: '1', name: '국밥집', position: LatLng(37.5, 127.0),
      category: '음식점', distanceMeters: 100,
    );
    await tester.pumpWidget(MaterialApp(
      home: Scaffold(
        body: InfoCard(
          poi: poi, onNavigate: () {},
          isSaved: false, onSave: () => saved = true,
        ),
      ),
    ));
    expect(find.text('저장'), findsOneWidget);
    await tester.tap(find.text('저장'));
    await tester.pump();
    expect(saved, isTrue);

    await tester.pumpWidget(MaterialApp(
      home: Scaffold(
        body: InfoCard(
          poi: poi, onNavigate: () {},
          isSaved: true, onSave: () {},
        ),
      ),
    ));
    expect(find.text('저장됨'), findsOneWidget);
  });
```

- [ ] **Step 2: 실패 확인**

Run: `flutter test test/ui/info_card_test.dart`
Expected: FAIL — `InfoCard`에 `isSaved`/`onSave` 없음.

- [ ] **Step 3: InfoCard에 저장 버튼 추가**

`lib/ui/info_card.dart`의 클래스 필드/생성자에 추가하고 버튼 한 개를 넣는다. 파일을 아래로 교체:
```dart
import 'package:flutter/material.dart';
import '../core/models/poi.dart';

/// 핀 탭 시 하단에 뜨는 정보 카드. 운전 중 가독성 위해 큰 글씨.
class InfoCard extends StatelessWidget {
  final Poi poi;
  final VoidCallback onNavigate;
  final bool isSaved;
  final VoidCallback onSave;
  const InfoCard({
    super.key,
    required this.poi,
    required this.onNavigate,
    this.isSaved = false,
    required this.onSave,
  });

  String get _distanceText {
    final d = poi.distanceMeters;
    if (d >= 1000) return '${(d / 1000).toStringAsFixed(1)}km';
    return '${d.round()}m';
  }

  @override
  Widget build(BuildContext context) {
    return Card(
      margin: const EdgeInsets.all(12),
      child: Padding(
        padding: const EdgeInsets.all(16),
        child: Column(
          mainAxisSize: MainAxisSize.min,
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text(poi.name,
                style: const TextStyle(
                    fontSize: 24, fontWeight: FontWeight.bold)),
            const SizedBox(height: 8),
            Text('$_distanceText · ${poi.category}',
                style: const TextStyle(fontSize: 18, color: Colors.black54)),
            const SizedBox(height: 16),
            Row(
              children: [
                Expanded(
                  child: OutlinedButton.icon(
                    onPressed: onSave,
                    icon: Icon(isSaved ? Icons.bookmark : Icons.bookmark_border),
                    label: Text(isSaved ? '저장됨' : '저장',
                        style: const TextStyle(fontSize: 18)),
                    style: OutlinedButton.styleFrom(
                        minimumSize: const Size.fromHeight(56)),
                  ),
                ),
                const SizedBox(width: 12),
                Expanded(
                  child: FilledButton(
                    onPressed: onNavigate,
                    style: FilledButton.styleFrom(
                        minimumSize: const Size.fromHeight(56)),
                    child: const Text('길찾기',
                        style: TextStyle(fontSize: 18)),
                  ),
                ),
              ],
            ),
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 4: 통과 확인**

Run: `flutter test test/ui/info_card_test.dart`
Expected: PASS (기존 + 신규).

- [ ] **Step 5: MapScreen 배선 (저장/해제 + auth gate + 저장 id 로드)**

`lib/ui/map_screen.dart` 수정:
(a) import 추가:
```dart
import '../saved/saved_place_repository.dart';
import 'login_sheet.dart';
```
(b) `MapScreen` 위젯에 필드/생성자 추가: `final SavedPlaceRepository savedRepo;` 와 `required this.savedRepo,`.
(c) `_MapScreenState`에 상태 추가:
```dart
  Set<String> _savedIds = {};
```
(d) `_start()`가 성공(권한 OK)한 직후, 로그인 상태면 저장 id 로드. `_start()`의 pipeline 구독 줄 다음에 추가:
```dart
    await _reloadSaved();
```
그리고 메서드 추가:
```dart
  Future<void> _reloadSaved() async {
    final u = widget.auth.user;
    if (u == null) {
      if (mounted) setState(() => _savedIds = {});
      return;
    }
    final ids = await widget.savedRepo.savedPlaceIds(u.id);
    if (mounted) setState(() => _savedIds = ids);
  }

  Future<void> _onSaveToggle(Poi poi) async {
    final u = widget.auth.user;
    if (u == null) {
      showLoginSheet(context, onGoogle: widget.auth.signInWithGoogle);
      return;
    }
    if (_savedIds.contains(poi.id)) {
      await widget.savedRepo.deleteByPlaceId(ownerId: u.id, placeId: poi.id);
    } else {
      await widget.savedRepo.save(ownerId: u.id, poi: poi);
    }
    await _reloadSaved();
  }
```
(e) InfoCard 호출에 저장 파라미터 연결. 기존 `InfoCard(poi: _selected!, onNavigate: ...)` 를:
```dart
              child: InfoCard(
                poi: _selected!,
                onNavigate: () => widget.navigationLauncher.launch(_selected!),
                isSaved: _savedIds.contains(_selected!.id),
                onSave: () => _onSaveToggle(_selected!),
              ),
```

- [ ] **Step 6: app.dart에서 savedRepo 주입**

`lib/app.dart`의 MapScreen 생성에 추가: import `import 'saved/saved_place_repository.dart';` 하고 MapScreen(...)에 `savedRepo: SavedPlaceRepository(),` 추가.

- [ ] **Step 7: 분석 + 테스트 + 커밋**

Run:
```bash
flutter analyze
flutter test
```
Expected: analyze 에러 없음, 전체 테스트 통과.
```bash
git add lib/ui/info_card.dart lib/ui/map_screen.dart lib/app.dart test/ui/info_card_test.dart
git commit -m "feat(saved): InfoCard 저장 버튼 + MapScreen 저장 배선(auth gate)"
```

---

## Task 4: 내 저장 목록 화면 + E2E

**Files:**
- Create: `lib/ui/saved_list_screen.dart`
- Modify: `lib/ui/map_screen.dart` (앱바에 목록 진입점)

**Interfaces:**
- Consumes: `SavedPlaceRepository`, `AuthController`, `SavedPlace`.
- Produces: `class SavedListScreen extends StatefulWidget { SavedListScreen({required repo, required ownerId}); }` — 내 저장 리스트 + 삭제.

- [ ] **Step 1: SavedListScreen 작성**

Create `lib/ui/saved_list_screen.dart`:
```dart
import 'package:flutter/material.dart';
import '../saved/saved_place.dart';
import '../saved/saved_place_repository.dart';

/// 내 저장 목록 화면.
class SavedListScreen extends StatefulWidget {
  final SavedPlaceRepository repo;
  final String ownerId;
  const SavedListScreen({super.key, required this.repo, required this.ownerId});

  @override
  State<SavedListScreen> createState() => _SavedListScreenState();
}

class _SavedListScreenState extends State<SavedListScreen> {
  late Future<List<SavedPlace>> _future;

  @override
  void initState() {
    super.initState();
    _future = widget.repo.listMine(widget.ownerId);
  }

  void _reload() {
    setState(() => _future = widget.repo.listMine(widget.ownerId));
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('내 저장')),
      body: FutureBuilder<List<SavedPlace>>(
        future: _future,
        builder: (context, snap) {
          if (snap.connectionState != ConnectionState.done) {
            return const Center(child: CircularProgressIndicator());
          }
          final items = snap.data ?? const [];
          if (items.isEmpty) {
            return const Center(child: Text('아직 저장한 곳이 없어요.'));
          }
          return ListView.separated(
            itemCount: items.length,
            separatorBuilder: (_, __) => const Divider(height: 1),
            itemBuilder: (context, i) {
              final p = items[i];
              return ListTile(
                leading: const Icon(Icons.bookmark, color: Color(0xFFFF5E13)),
                title: Text(p.name),
                subtitle: Text([
                  if (p.category != null) p.category!,
                  if (p.memo != null && p.memo!.isNotEmpty) p.memo!,
                ].join(' · ')),
                trailing: IconButton(
                  icon: const Icon(Icons.delete_outline),
                  onPressed: () async {
                    if (p.placeId != null) {
                      await widget.repo.deleteByPlaceId(
                          ownerId: widget.ownerId, placeId: p.placeId!);
                      _reload();
                    }
                  },
                ),
              );
            },
          );
        },
      ),
    );
  }
}
```

- [ ] **Step 2: MapScreen 앱바에 진입점 추가**

`lib/ui/map_screen.dart` 앱바 `actions:`의 tune 아이콘 앞(또는 뒤)에 북마크 버튼 추가. import `import 'saved_list_screen.dart';` 하고:
```dart
          IconButton(
            icon: const Icon(Icons.bookmarks_outlined),
            tooltip: '내 저장',
            onPressed: () {
              final u = widget.auth.user;
              if (u == null) {
                showLoginSheet(context, onGoogle: widget.auth.signInWithGoogle);
                return;
              }
              Navigator.of(context).push(MaterialPageRoute(
                builder: (_) => SavedListScreen(
                    repo: widget.savedRepo, ownerId: u.id),
              ));
            },
          ),
```

- [ ] **Step 3: 분석 + 테스트**

Run: `flutter analyze` + `flutter test`
Expected: 에러 없음, 전체 통과.

- [ ] **Step 4: 실기기 E2E (사용자 Supabase에 0002 SQL 적용 후)**

에뮬레이터 또는 폰에서:
```bash
export PATH="$HOME/Library/Android/sdk/platform-tools:/opt/homebrew/bin:$PATH"
cd ~/dev/odagada && flutter run -d <device>
```
확인 항목:
- [ ] 로그인 상태에서 핀 탭 → 정보 카드에 **저장** 버튼.
- [ ] **저장** 탭 → 버튼이 **"저장됨"**(북마크 채워짐)으로.
- [ ] 앱바 **북마크 아이콘 → 내 저장 목록**에 그 장소가 보임.
- [ ] 목록에서 **삭제** → 사라짐. 지도 핀은 다시 "저장"으로.
- [ ] 익명 상태에서 저장 탭 → **로그인 시트** 뜸.
- [ ] Supabase Table Editor `saved_places`에 행 생성/삭제 확인.

- [ ] **Step 5: 커밋**

```bash
git add lib/ui/saved_list_screen.dart lib/ui/map_screen.dart
git commit -m "feat(saved): 내 저장 목록 화면 + 진입점 (v2.0b 저장 완성)"
```

---

## Self-Review (작성자 점검)

- **스펙 커버리지 (v2.0b 저장 핵심):**
  - 저장(핀탭 → 저장 + 메모 없이 기본) → Task 3. ✅ (메모 입력 UI는 후속 — 이번엔 memo=null로 저장)
  - saved_places + RLS(본인만) → Task 0. ✅
  - 저장 로그인 필요/익명이면 로그인 유도 → Task 3 auth gate. ✅
  - 내 저장 목록 + 삭제 → Task 4. ✅
  - is_public 기본 true(친구 공개) → Task 0 DB 기본값. ✅
  - (후속: 한줄 메모 입력 UI, 검색 저장, 직접추가 long-press, 7종 카테고리 버킷, 지도 저장 핀 레이어, 겹침 3상태(v2.1 소셜).)
- **플레이스홀더 스캔:** TBD/TODO 없음. 코드 스텝에 실제 코드 포함. (사용자 SQL 적용은 외부 설정.) ✅
- **타입 일관성:** `SavedPlace`, `savedPlaceFromRow/Insert`, `SavedPlaceRepository.save/deleteByPlaceId/listMine/savedPlaceIds`, `InfoCard(isSaved,onSave)`, `MapScreen(savedRepo:)`, `SavedListScreen(repo,ownerId)` 일치. Poi.id = place_id 일관. ✅
- **통합 검증:** Repository는 Supabase 래퍼라 Task 4 E2E로 검증(순수 매핑은 Task 1). ✅

---

## 후속 계획
- **v2.0b-2**: 한줄 메모 입력 UI, 검색 저장, 직접추가(long-press), 7종 카테고리 버킷 매핑, 지도에 내 저장 핀 레이어.
- **v2.1**: 친구 초대(딥링크) + 친구 저장 레이어("N명", 핀 3상태 겹침) + 통합 정보카드 + 주행 추천 전환.

---

## 실행 방법
1. **Subagent-Driven (권장)** — 태스크마다 새 서브에이전트 + 리뷰.
2. **Inline Execution** — 체크포인트 단위 실행.
