# v2.1b-2 통합 정보카드 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 핀 탭 시 정보카드에 "내가 저장(+내 메모)"와 "친구 N명 저장 — 이름별 메모"를 보여준다.

**Architecture:** 데이터는 v2.1b-1의 `friend_saves_near`(친구 이름·메모 반환)와 `saved_places.memo`에 이미 있다. `friendOverlay`에 center를 넘겨 친구-only 핀의 거리(현재 0)를 보정하고, `MapScreen`이 친구 저장을 place_id로 보관해 핀 탭 시 카드에 이름·메모·내 메모를 전달한다. `InfoCard`는 옵션 파라미터로 두 섹션을 렌더한다.

**Tech Stack:** Flutter 3.44.6, 기존 v2.1b-1 소셜 레이어 재사용. 새 DB·의존성 없음.

## Global Constraints
- 저장소 `/Users/goodbug/dev/odagada`, 브랜치 `feature/v2b-save`(현재), 새 브랜치 금지. Flutter `/opt/homebrew/bin`(`export PATH="/opt/homebrew/bin:$PATH"`). 패키지 `odagada`.
- 기존 테스트 회귀 유지(현재 73). 순수/위젯 TDD. `flutter analyze` 신규 0(기존 info 3건).
- 친구 강조색 `#1B4F9B`(v2.1b-1 배지·링과 동일).
- 마이그레이션/DB 변경 없음. 표시·배선만.
- 평점/리뷰 없음(원칙). 딥링크는 v2.1c.

## 파일 구조
- Modify `lib/friends/friend_save.dart`(friendOverlay center 인자) + `test/friends/friend_save_test.dart`
- Modify `lib/ui/info_card.dart`(친구·내메모 섹션) + `test/ui/info_card_test.dart`
- Modify `lib/ui/map_screen.dart`(_friendSaves 보관 + 카드 배선)

---

### Task 1: friendOverlay 거리 보정(center 인자)

**Files:**
- Modify: `lib/friends/friend_save.dart`
- Modify: `test/friends/friend_save_test.dart`

**Interfaces:**
- Produces: `({List<Poi> extraPins, Map<String,int> friendCounts}) friendOverlay(List<FriendSave> friendSaves, Set<String> existingIds, LatLng center)` — extraPin `distanceMeters = GeoMath.distanceMeters(center, pos)`.

- [ ] **Step 1: 테스트 갱신(실패 유도)**

`test/friends/friend_save_test.dart`의 `friendOverlay` 테스트를 center 인자·거리 검증으로 교체:
```dart
  test('friendOverlay: 카운트 전부, extraPins는 기존에 없는 것만, 거리는 center 기준', () {
    final saves = [
      FriendSave(placeId: 'a', lat: 37.5, lng: 127.0, name: 'A',
          bucket: PlaceCategory.cafe, friendCount: 1, friendNames: ['철수'], memos: ['']),
      FriendSave(placeId: 'b', lat: 37.6, lng: 127.1, name: 'B',
          bucket: PlaceCategory.bar, friendCount: 3, friendNames: [], memos: []),
    ];
    final r = friendOverlay(saves, {'a'}, const LatLng(37.5, 127.0));
    expect(r.friendCounts, {'a': 1, 'b': 3});
    expect(r.extraPins.map((p) => p.id).toList(), ['b']);
    expect(r.extraPins.single.distanceMeters, greaterThan(0)); // center(37.5)↔b(37.6) ~11km
  });
```
(상단에 `import 'package:odagada/core/models/lat_lng.dart';`가 없으면 추가.)

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/goodbug/dev/odagada && flutter test test/friends/friend_save_test.dart`
Expected: FAIL(인자 수 불일치 컴파일 에러).

- [ ] **Step 3: 구현**

`lib/friends/friend_save.dart`:
(a) import 추가: `import '../core/geo/geo_math.dart';`
(b) `friendOverlay` 시그니처·본문:
```dart
({List<Poi> extraPins, Map<String, int> friendCounts}) friendOverlay(
    List<FriendSave> friendSaves, Set<String> existingIds, LatLng center) {
  final counts = <String, int>{};
  final extra = <Poi>[];
  for (final fs in friendSaves) {
    counts[fs.placeId] = fs.friendCount;
    if (!existingIds.contains(fs.placeId)) {
      final pos = LatLng(fs.lat, fs.lng);
      extra.add(Poi(
        id: fs.placeId,
        name: fs.name,
        position: pos,
        category: fs.bucket.label,
        bucket: fs.bucket,
        distanceMeters: GeoMath.distanceMeters(center, pos),
      ));
    }
  }
  return (extraPins: extra, friendCounts: counts);
}
```

- [ ] **Step 4: 통과 + 회귀**

Run: `cd /Users/goodbug/dev/odagada && flutter test && flutter analyze`
Expected: 전건 PASS, analyze 신규 0. (MapScreen의 `friendOverlay(...)` 호출은 인자 2개라 컴파일 에러가 남 — Task 3에서 center 전달로 수정. 이 태스크는 `flutter test test/friends/friend_save_test.dart` 통과 + analyze의 신규 에러가 오직 `map_screen.dart`의 friendOverlay 인자 개수뿐인지로 확인.)

- [ ] **Step 5: 커밋**
```bash
cd /Users/goodbug/dev/odagada
git add lib/friends/friend_save.dart test/friends/friend_save_test.dart
git commit -m "feat: friendOverlay computes friend-only pin distance from center"
```

---

### Task 2: InfoCard — 내 저장·친구 섹션

**Files:**
- Modify: `lib/ui/info_card.dart`
- Modify: `test/ui/info_card_test.dart`

**Interfaces:**
- Produces: `InfoCard`에 옵션 파라미터 `String? myMemo`, `List<String>? friendNames`, `List<String>? friendMemos` 추가. `isSaved`면 "내가 저장(+myMemo)", `friendNames` 있으면 "친구 N명 저장" + 이름별 메모 섹션.

- [ ] **Step 1: 실패하는 위젯 테스트 추가**

`test/ui/info_card_test.dart`에 추가(기존 Poi 생성·pump 패턴 재사용):
```dart
  testWidgets('친구 저장 섹션: N명 + 이름별 메모', (t) async {
    final poi = const Poi(
      id: 'p1', name: '금양화로', position: LatLng(37.5, 127.0),
      category: '맛집', bucket: PlaceCategory.restaurant, distanceMeters: 50);
    await t.pumpWidget(MaterialApp(home: Scaffold(body: InfoCard(
      poi: poi, onNavigate: () {}, onSave: () {}, onClose: () {},
      friendNames: const ['철수', '영희'], friendMemos: const ['여기 좋아요', ''],
    ))));
    expect(find.textContaining('친구 2명'), findsOneWidget);
    expect(find.textContaining('철수'), findsOneWidget);
    expect(find.textContaining('여기 좋아요'), findsOneWidget);
    expect(find.text('영희'), findsOneWidget); // 메모 없으면 이름만
  });

  testWidgets('내 저장 + 내 메모 섹션', (t) async {
    final poi = const Poi(
      id: 'p1', name: '금양화로', position: LatLng(37.5, 127.0),
      category: '맛집', bucket: PlaceCategory.restaurant, distanceMeters: 50);
    await t.pumpWidget(MaterialApp(home: Scaffold(body: InfoCard(
      poi: poi, onNavigate: () {}, onSave: () {}, onClose: () {},
      isSaved: true, myMemo: '내가 왔던 곳',
    ))));
    expect(find.textContaining('내가 저장'), findsOneWidget);
    expect(find.textContaining('내가 왔던 곳'), findsOneWidget);
  });
```
(파일 상단에 `place_category.dart`/`lat_lng.dart` import가 이미 있으면 재사용.)

- [ ] **Step 2: 실패 확인**

Run: `cd /Users/goodbug/dev/odagada && flutter test test/ui/info_card_test.dart`
Expected: FAIL(파라미터·섹션 없음).

- [ ] **Step 3: InfoCard 구현**

`lib/ui/info_card.dart`:
(a) 파일 상단에 색 상수(기존 import 아래): `const Color _friendBlue = Color(0xFF1B4F9B);`
(b) 생성자에 파라미터 추가:
```dart
  final String? myMemo;
  final List<String>? friendNames;
  final List<String>? friendMemos;
  const InfoCard({
    super.key,
    required this.poi,
    required this.onNavigate,
    this.isSaved = false,
    required this.onSave,
    required this.onClose,
    this.myMemo,
    this.friendNames,
    this.friendMemos,
  });
```
(c) 스크롤 Column의 영업시간 블록 뒤(고정 푸터 위)에 섹션 추가:
```dart
              if (isSaved) ...[
                const SizedBox(height: 12),
                Row(children: [
                  const Icon(Icons.check_circle,
                      size: 18, color: Color(0xFFFF5E13)),
                  const SizedBox(width: 6),
                  const Text('내가 저장',
                      style: TextStyle(fontWeight: FontWeight.bold)),
                ]),
                if (myMemo != null && myMemo!.isNotEmpty)
                  Padding(
                    padding: const EdgeInsets.only(top: 2, left: 24),
                    child: Text('"$myMemo"',
                        style: const TextStyle(color: Colors.black54)),
                  ),
              ],
              if (friendNames != null && friendNames!.isNotEmpty) ...[
                const SizedBox(height: 12),
                Row(children: [
                  const Icon(Icons.group, size: 18, color: _friendBlue),
                  const SizedBox(width: 6),
                  Text('친구 ${friendNames!.length}명 저장',
                      style: const TextStyle(
                          fontWeight: FontWeight.bold, color: _friendBlue)),
                ]),
                ...List.generate(friendNames!.length, (i) {
                  final name = friendNames![i];
                  final memo = (friendMemos != null && i < friendMemos!.length)
                      ? friendMemos![i]
                      : '';
                  return Padding(
                    padding: const EdgeInsets.only(top: 2, left: 24),
                    child: Text(memo.isEmpty ? name : '$name — "$memo"',
                        style: const TextStyle(color: Colors.black87)),
                  );
                }),
              ],
```
(주의: 이 블록은 스크롤되는 `SingleChildScrollView`의 Column 자식으로, 영업시간(`poi.weekdayHours` 블록) 다음·`const SizedBox(height: 16)`나 푸터 이전 위치에 넣는다. 정확한 위치는 기존 파일에서 영업시간 블록 끝을 찾아 그 뒤에.)

- [ ] **Step 4: 통과 확인**

Run: `cd /Users/goodbug/dev/odagada && flutter test test/ui/info_card_test.dart`
Expected: PASS(신규 2건 포함).

- [ ] **Step 5: 커밋**
```bash
cd /Users/goodbug/dev/odagada
git add lib/ui/info_card.dart test/ui/info_card_test.dart
git commit -m "feat(card): my-save memo + friends-who-saved sections"
```

---

### Task 3: MapScreen — 친구 저장 보관 + 카드 배선

**Files:**
- Modify: `lib/ui/map_screen.dart`

**Interfaces:**
- Consumes: `FriendSave`(placeId/friendNames/memos), `friendOverlay(...,center)`(Task1), InfoCard 새 파라미터(Task2).

- [ ] **Step 1: `_friendSaves` 보관 + friendOverlay center 전달**

`lib/ui/map_screen.dart`:
(a) 상태 필드 추가(`_savedPlaces` 근처):
```dart
  Map<String, FriendSave> _friendSaves = const {};
```
(b) `_runSearch`에서 friendOverlay 호출을 center 전달로 바꾸고, 받은 friendSaves를 map으로 보관. mounted 가드 뒤에:
```dart
    final social = friendOverlay(friendSaves, existing, center);
    _friendSaves = {for (final fs in friendSaves) fs.placeId: fs};
```
(기존 `final social = friendOverlay(friendSaves, existing);` 한 줄을 위 두 줄로 교체.)

- [ ] **Step 2: 내 메모 조회 헬퍼 + InfoCard 배선**

(a) 헬퍼 메서드 추가(`_onPinTap` 근처):
```dart
  String? _myMemoFor(String placeId) {
    for (final p in _savedPlaces) {
      if (p.placeId == placeId) return p.memo;
    }
    return null;
  }
```
(b) InfoCard 생성에 파라미터 추가(기존 `onClose:` 뒤):
```dart
              child: InfoCard(
                poi: _selected!,
                onNavigate: () => widget.navigationLauncher.launch(_selected!),
                isSaved: _savedIds.contains(_selected!.id),
                onSave: () => _onSaveToggle(_selected!),
                onClose: () => setState(() => _selected = null),
                myMemo: _myMemoFor(_selected!.id),
                friendNames: _friendSaves[_selected!.id]?.friendNames,
                friendMemos: _friendSaves[_selected!.id]?.memos,
              ),
```

- [ ] **Step 3: 분석 + 회귀**

Run: `cd /Users/goodbug/dev/odagada && flutter analyze && flutter test`
Expected: analyze 신규 0(friendOverlay 인자 에러 해소), 테스트 green.

- [ ] **Step 4: 커밋**
```bash
cd /Users/goodbug/dev/odagada
git add lib/ui/map_screen.dart
git commit -m "feat: pass friend names/memos + my memo to info card on pin tap"
```

---

## 온디바이스 E2E(테스트 친구 데이터 사용)
1. 폰에서 "테스트 친구맛집" 핀 탭 → 카드에 **"친구 1명 저장 — 테스트친구 — 여기 진짜 맛있어요"** + (내가 저장했으면) **"내가 저장"** 섹션.
2. 거리도 0m가 아니라 실제 거리로.

## Self-Review 결과
- Spec 커버리지: 거리 보정(Task1)·카드 섹션(Task2)·배선(Task3). 통합카드 완료. 딥링크 제외.
- Placeholder: 없음.
- 타입 일관성: `friendOverlay(...,LatLng center)`(Task1)를 Task3가 호출, `InfoCard(myMemo,friendNames,friendMemos)`(Task2)를 Task3가 채움, `_friendSaves: Map<String,FriendSave>`. friendOverlay 시그니처 변경이 Task1(정의·테스트)·Task3(호출) 모두 반영.
